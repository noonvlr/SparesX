# Local env for chat (do not commit secrets)

# Already used by the app (your .env may use MONGO_URI — both work):
# MONGODB_URI=...
# JWT_SECRET=...

# Socket.IO companion server
SOCKET_PORT=4001
SOCKET_CORS_ORIGIN=http://localhost:3000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4001
