# Layout

**Import:** `@/components/layout`

| Shell | Use |
|-------|-----|
| `MarketplacePage` | Browse / home / product grids (`container` xl) |
| `DashboardPage` | Buyer / technician dashboards |
| `AuthPage` | Login / register / verify |
| `AdminPage` | Admin surfaces |

Also: `Container`, `Stack`, `SectionHeader` from `@/components/ui/Container`.

App chrome remains `AppShell` — these wrappers only standardize page padding/background.
