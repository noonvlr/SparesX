# Select / Checkbox / Switch / Label

| Component | Import |
|-----------|--------|
| Select | `@/components/ui/Select` |
| Checkbox | `@/components/ui/Checkbox` |
| Switch | `@/components/ui/Switch` (Radix) |
| Label | `@/components/ui/Label` |

Select mirrors Input variants (`default` | `error`) and sizes. Prefer `Field` + `Select` for admin CRUD filters.
