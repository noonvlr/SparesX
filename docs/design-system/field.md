# Field

**Import:** `@/components/ui/Field`

## Purpose

Labeled control wrapper: label, required marker, hint, and error.

## Props

`label`, `htmlFor`, `hint`, `error`, `required`, `children`

## Do

Always use for marketplace / admin forms instead of ad-hoc `<label>` + muted hint paragraphs.
